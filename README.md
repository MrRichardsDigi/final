# Small Business Static Site

Simple static website scaffold (HTML/CSS/JS).

Open `index.html` in a browser or serve with a static server.

Example quick serve (Python 3):

```bash
python -m http.server 8000

# then open http://localhost:8000
```
# Pocksy
# Pocksy
# Pocksy
# Pocksy
